# Secrets and shared environment for supervised data/training jobs.
# Keep this file root-owned and mode 0640. Never commit real tokens.
# Uncomment only when a real token is required; an empty HF_TOKEN produces an
# invalid `Authorization: Bearer ` header in huggingface_hub.
# HF_TOKEN=hf_...
